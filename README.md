# Wordpress docker Xdebug VS code debugging
 WordPress Docker image with Xdebug for debugging via VS Code

```
new_project.sh somename
cd TemplateDocker/docker
docker-compose up
```

Use docker with WSL or Linux.
